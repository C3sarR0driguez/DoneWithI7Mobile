import { StyleSheet } from 'react-native';

const baseStyles = {};

const styles = StyleSheet.create(baseStyles);

export { baseStyles };
export default styles;
import { StyleSheet } from 'react-native';

const baseStyles = {};

const styles = StyleSheet.create(baseStyles);

export { baseStyles };
export default styles;
