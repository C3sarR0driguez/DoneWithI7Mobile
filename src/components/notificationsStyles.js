import { StyleSheet } from "react-native";

const baseStyles = {
    root: {
        position: "absolute",
        top: 0,
        left: 0,
        right: 0
    },
};

const styles = StyleSheet.create(baseStyles);
export { baseStyles };
export default styles;
